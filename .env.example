COORDINATOR_MODE=observe
LISTEN_HOST=0.0.0.0
LISTEN_PORT=8787

SOFT_FLOOR_MB=3072
HARD_FLOOR_MB=1536
SAFETY_OVERHEAD_MB=768

LEASE_TTL_SECONDS=300
MAX_QUEUE_DEPTH=20
DEFAULT_DEADLINE_SECONDS=30

REQUIRE_API_TOKEN=false
API_TOKEN=
ENFORCE_ALLOWLIST=false
ALLOWED_CALLERS=

LOG_LEVEL=INFO

# Staging profiles
# observe profile:
#   COORDINATOR_MODE=observe
# enforce-low-tier profile (still shedded under soft pressure):
#   COORDINATOR_MODE=enforce
#   SOFT_FLOOR_MB=3072
#   HARD_FLOOR_MB=1536
# enforce-all profile:
#   COORDINATOR_MODE=enforce
#   SOFT_FLOOR_MB=4096
#   HARD_FLOOR_MB=2048